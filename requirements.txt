# Runtime dependencies
numpy>=1.23
scipy>=1.9
matplotlib>=3.6
pandas>=1.5
openpyxl>=3.1
PySide6>=6.5
soundfile>=0.12
sounddevice>=0.4

# Test dependency
pytest>=7.0

# System dependency for reading misnamed/non-WAV media files:
# install FFmpeg so both `ffmpeg` and `ffprobe` are available on PATH.
# macOS: brew install ffmpeg
